name: CI

on:
  push:
  pull_request:

jobs:
  build-job:
    runs-on: ubuntu-latest
    steps:
      - name: Build project
        run: echo "Building project..."

  test-job:
    runs-on: ubuntu-latest
    needs: build-job
    steps:
      - name: Run tests
        run: echo "Running tests..."